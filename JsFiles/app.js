// Initialise le popover pour les éléments avec la classe .popover-dismiss
const popover = new bootstrap.Popover('.message-popover', {
    trigger: 'focus'
});

const popover = new bootstrap.Popover('.message-popover2', {
    trigger: 'focus'
});